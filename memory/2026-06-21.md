# 2026-06-21

- Jerry 继续推进“家庭成员可复用的 GP 预约流程”的权限边界设计，明确希望其他家庭成员“只能聊天、调用已有 skills”。
- 已核实现状：OpenClaw / Telegram 当前更适合做“群组或 topic 级别”的 skills/tools 限权；文档里确认有 `channels.telegram.groups.<id>.skills`、`tools`、`toolsBySender` 等配置。
- 当前未确认到 Telegram 私聊场景下按单个家庭成员做细粒度 `skills/tools` 白名单的原生支持；私聊侧看到的主要是 `allowFrom`、`historyLimit` 一类控制。
- 给 Jerry 的落地建议：他自己的私聊保留高权限，其他家庭成员放到专用 Telegram 群或 topic 中，只开放如 `gp-booking` 这类已有 skill，并禁用高风险工具；如果要更强隔离，建议单独开一个 family bot / 第二个 Telegram account。
