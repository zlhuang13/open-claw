# 2026-06-25

- 今天排查并修正了 OpenClaw 的多 Telegram bot / 多 agent 路由问题。关键结论：`family` agent 本来就被配置为低权限，`write`、`exec`、`edit`、`apply_patch` 等工具被 deny，不是模型随机坏掉。
- 目标路由约定：`JerryAI_ClawBot` 应接 `main` / `default` account，`HuangAI_bot` 应接 `family` account。为避免同一 Telegram 用户在两个 bot 间串会话，重点关注 `session.dmScope` 的按 account 隔离配置。
- 用户明确要求：`family` 必须是独立 workspace、全新记忆，不继承之前的记忆/SOUL/用户资料；`family` 的 skill 只保留 `gp-booking`。
- 用户明确要求：定时任务（尤其 daily news / bin collection 一类主动消息）必须回到 Jerry 对应的 `JerryAI_ClawBot` 对话，不要发到 `family` / `HuangAI_bot`。
- 做过一次完整 bin collection cron 投递测试，结论是消息应落到 `main` / Jerry 这边，用来验证 cron -> agent -> Telegram account 的完整链路。
- 项目远端备份仓库确定为：`https://github.com/zlhuang13/open-claw.git`。
- 已为该仓库配置 SSH 推送并成功 push；远端最新已推送提交为 `a0b53f1` (`Create isolated family workspace`)。同时提醒过用户：本地仍有未提交改动，没有全部一起推上去。
